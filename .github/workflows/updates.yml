name: Kubeerneets Application

on:
  push:
    branches:
      - kuberneetes-application

jobs:
    build:
        runs-on: ubuntu-latest
    
        steps:
        - name: Checkout code
          uses: actions/checkout@v2
    
        - name: Build Docker image
          run: echo "Current run number is $GITHUB_RUN_NUMBER"